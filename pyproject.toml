[project]
name = "mortgage-payoff"
version = "2.0.0"
description = "Monte Carlo mortgage payoff simulation backed by DuckDB"
requires-python = ">=3.10"
dependencies = [
    "duckdb>=1.3",
    "matplotlib>=3.8",
    "numpy>=1.26",
]

[project.scripts]
mortgage-payoff = "mortgage_payoff:main"
